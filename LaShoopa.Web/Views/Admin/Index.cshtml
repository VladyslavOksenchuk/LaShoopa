@model IEnumerable<LaShoopa.Web.Models.DTOs.ProductDTO>

<h1>Admin Panel</h1>

<div class="container">
	<div class="d-flex justify-content-end"><a asp-action="CreateProduct" class="btn btn-primary">Add product</a></div>
	<br />
	<h3>List</h3>
	@* <div class="row row-cols-3">
	@foreach (var item in Model)
	{
	<div class="card m-2" style="width: 18rem;">
	<img src="@item.ImageUrl" class="card-img-top" alt="@item.Name">
	<div class="card-body">
	<h5 class="card-title">@item.Name</h5>
	<p class="card-text">@item.Description</p>
	<p class="card-text">@item.Price</p>
	</div>
	</div>
	}
	</div> *@
	<table class="table table-striped table-hover m-3">
		<thead>
			<tr>
				<th scope="col">Id</th>
				<th scope="col">Name</th>
				<th scope="col">Price</th>
				<th scope="col">Description</th>
				<th scope="col">Update/Delete</th>
			</tr>
		</thead>
		<tbody>
			@foreach (var item in Model)
			{
				<tr>
					<th scope="row">@item.Id</th>
					<td>@item.Name</td>
					<td>@item.Price</td>
					<td>@item.Description</td>
					<td>
						<a class="btn btn-info" asp-action="UpdateProduct" asp-route-productId="@item.Id">Update</a>
						<a class="btn btn-danger" asp-action="DeleteProduct" asp-route-productId="@item.Id">Delete</a>
					</td>
				</tr>
			}
		</tbody>
	</table>
</div>